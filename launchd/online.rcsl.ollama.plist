<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>online.rcsl.ollama</string>

	<!-- OLLAMA_HOST lives here rather than in `launchctl setenv`, which is
	     scoped to the boot session: after a reboot the variable is gone and
	     ollama falls back to 0.0.0.0, publishing inference to the LAN with
	     nothing to indicate it. security.md 7.1 requires the loopback bind,
	     so the bind has to survive a reboot on its own.
	     The other two carry over from the Homebrew formula's own plist. -->
	<key>EnvironmentVariables</key>
	<dict>
		<key>OLLAMA_HOST</key>
		<string>127.0.0.1</string>
		<key>OLLAMA_FLASH_ATTENTION</key>
		<string>1</string>
		<key>OLLAMA_KV_CACHE_TYPE</key>
		<string>q8_0</string>

		<!-- Both are needed and they are not the same thing. OLLAMA_MODELS is
		     where the weights live; HOME is where everything else ollama writes
		     goes, including the id_ed25519 it signs pulls with. Setting only
		     the first leaves the second at the service account's default home,
		     which this account does not have. -->
		<key>HOME</key>
		<string>/Users/Shared/ollama</string>
		<key>OLLAMA_MODELS</key>
		<string>/Users/Shared/ollama/models</string>
	</dict>

	<key>ProgramArguments</key>
	<array>
		<string>/opt/homebrew/opt/ollama/bin/ollama</string>
		<string>serve</string>
	</array>

	<!-- The dedicated non-administrator account security.md 7.1(d) asks for,
	     adopted 2026-08-18 by launchd/adopt-ollama-service-account.sh.
	     `_rcslollama` is not in `admin` and cannot sudo, which is the whole
	     point: this process loads weights downloaded from the internet, and
	     the format rule on that download was only enforced the same day.

	     It ran as `rcslmac1` until then — an admin login — because a daemon
	     defaults to root, root looks for models in /var/root/.ollama and finds
	     none, and running as the operator was the change that avoided root
	     without moving the model directory. Moving the directory is what
	     unblocked it: /Users/rcslmac1 is mode 750, so no account outside
	     `staff` can traverse into it, and the weights had to leave the home
	     directory before any service account could read them. They now live in
	     /Users/Shared/ollama, owned by this account and group-readable by
	     `staff` so that Docker can still bind-mount them read-only for the
	     tokenizer. -->
	<key>UserName</key>
	<string>_rcslollama</string>

	<!-- A LaunchDaemon, not a LaunchAgent: this machine is headless and runs
	     24/7, so the runtime must come back after a power cut without anyone
	     logging in. -->
	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<true/>

	<key>StandardOutPath</key>
	<string>/opt/homebrew/var/log/ollama.log</string>
	<key>StandardErrorPath</key>
	<string>/opt/homebrew/var/log/ollama.log</string>
	<key>WorkingDirectory</key>
	<string>/opt/homebrew/var</string>
</dict>
</plist>
